import streamlit as st
import pandas as pd
import plotly.express as px

# 1. ตั้งค่าหัวข้อแอป
st.title("📊 RTE Sales Dashboard")

# 2. รายชื่อสินค้าที่คุณต้องการ (Master Data)
target_items = {
    "203081": "ฮ็อทด็อก", "250561": "ขนมจีบหมู ชุดใหญ่", "274583": "ชุดรวมของทอด",
    "299207": "ไก่ป๊อปชุดใหญ่", "381059": "นักเก็ตไก่คลาสสิค", "395441": "ฮะเก๋าชุดใหญ่",
    "614329": "เกี๊ยวซ่าหมูกุยช่าย ชุดใหญ่", "619903": "ปีกไก่บนคลุกซอส",
    "648962": "ฮะเก๋าโปรโมชั่น", "779278": "ไก่ป๊อป", "782617": "เกี๊ยวซ่าหมูชุดใหญ่",
    "956994": "นักเก็ตชุดใหญ่"
}

# 3. ปุ่มอัปโหลดรูป (ในแอปจริงจะต้องต่อกับระบบ OCR แต่ตอนนี้เราทำระบบบันทึกค่าก่อน)
st.sidebar.header("เมนูจัดการ")
date_input = st.sidebar.date_input("เลือกวันที่ขาย")
uploaded_file = st.sidebar.file_uploader("อัปโหลดรูปยอดขาย")

# ส่วนจำลองการคีย์ข้อมูล (เพื่อให้แอปทำงานได้ทันที)
st.subheader("📝 กรอกข้อมูลจากรูป")
art_no = st.selectbox("เลือกรหัสสินค้า", list(target_items.keys()))
qty = st.number_input("จำนวน (Qty Sum)", min_value=0.0)
amt = st.number_input("ยอดเงิน (Amount SUM)", min_value=0.0)

if st.button("บันทึกข้อมูล"):
    # คำนวณ Vat 7% อัตโนมัติ
    vat_amt = amt * 1.07
    st.success(f"บันทึก {target_items[art_no]} ยอดรวม Vat: {vat_amt:,.2f} บาท เรียบร้อย!")
