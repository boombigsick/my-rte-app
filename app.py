import streamlit as st
import pandas as pd
import plotly.graph_objects as go
import easyocr
import numpy as np
from PIL import Image

# --- 1. CONFIG & CLEAN THEME ---
st.set_page_config(page_title="Sales Report", layout="wide")

# ปรับให้เป็นพื้นขาว ตัวหนังสือดำ 100% เพื่อความชัดเจน
st.markdown("""
    <style>
    .stApp { background-color: #FFFFFF; }
    h1, h2, h3, p, b, label, .stMetricValue, .stMetricLabel { color: #111111 !important; }
    div[data-testid="stMetric"] { 
        background-color: #F8F9FA; 
        border: 1px solid #E0E0E0; 
        border-radius: 8px; 
    }
    .stTable { background-color: white; }
    </style>
    """, unsafe_allow_html=True)

# --- 2. MASTER DATA ---
CATEGORY_MAP = {
    "203081": "ทานเล่น", "250561": "ทานเล่น", "274583": "ทานเล่น", "299207": "ทานเล่น",
    "381059": "ทานเล่น", "395441": "ทานเล่น", "614329": "ทานเล่น", "619903": "ทานเล่น",
    "648962": "ทานเล่น", "779278": "ทานเล่น", "782617": "ทานเล่น", "956994": "ทานเล่น",
    "231259": "พร้อมทาน", "302490": "พร้อมทาน", "322224": "พร้อมทาน", "344174": "พร้อมทาน",
    "364882": "พร้อมทาน", "380450": "พร้อมทาน", "621822": "พร้อมทาน", "654830": "พร้อมทาน",
    "695884": "พร้อมทาน", "724276": "พร้อมทาน", "781110": "พร้อมทาน", "951651": "พร้อมทาน",
    "250271": "พร้อมทาน", "273023": "พร้อมทาน", "967970": "พร้อมทาน"
}

PRODUCT_NAMES = {
    "203081": "ฮ็อทด็อก", "250561": "ขนมจีบหมู ชุดใหญ่", "274583": "ชุดรวมของทอด",
    "299207": "ไก่ป๊อปชุดใหญ่", "381059": "นักเก็ตไก่คลาสสิค", "395441": "ฮะเก๋าชุดใหญ่",
    "614329": "เกี๊ยวซ่าหมูกุยช่าย ชุดใหญ่", "619903": "ปีกไก่บนคลุกซอส",
    "648962": "ฮะเก๋าโปรโมชั่น", "779278": "ไก่ป๊อป", "782617": "เกี๊ยวซ่าหมูชุดใหญ่",
    "956994": "นักเก็ตชุดใหญ่", "231259": "เนื้อเป็ดย่างเครื่องเทศ", 
    "302490": "บะหมี่หมูแดงใหญ่", "322224": "ยำหมูกรอบ", "344174": "ขาหมูเยอรมัน", 
    "364882": "หมูแดง", "380450": "บะหมี่เป็ดย่างใหญ่", "621822": "อาหารพร้อมทาน 99 บาท", 
    "654830": "เป็ดพะโล้พร้อมไส้", "695884": "หมูกรอบ แพ็คใหญ่", 
    "724276": "ขาหมูพะโล้ (เลาะเนื้อ)", "781110": "บะหมี่เป็ดพะโล้ใหญ่", 
    "951651": "กุ้งต้ม", "250271": "บะหมี่เป็ดย่าง", 
    "273023": "บะหมี่หมูแดงสไตล์ฮ่องกง", "967970": "บะหมี่เนื้อเป็ดพะโล้"
}

@st.cache_resource
def get_reader():
    return easyocr.Reader(['th', 'en'])

reader = get_reader()

# --- 3. SIDEBAR ---
with st.sidebar:
    st.header("📋 Menu")
    page = st.radio("เลือกหน้า", ["📊 สรุปภาพรวม", "🍟 หมวดทานเล่น", "🍱 หมวดพร้อมทาน"])
    uploaded_file = st.file_uploader("อัปโหลดรูปยอดขาย", type=['jpg', 'png', 'jpeg'])

if uploaded_file:
    image = Image.open(uploaded_file)
    img_np = np.array(image)
    result = reader.readtext(img_np)
    full_text = [res[1] for res in result]
    
    raw_data = {}
    for i, text in enumerate(full_text):
        code = text.replace(" ", "").strip()
        if code in CATEGORY_MAP:
            try:
                qty = float(full_text[i+2].replace(",", ""))
                amt = float(full_text[i+3].replace(",", ""))
                raw_data[code] = {"q": qty, "a": amt}
            except: continue

    # --- กระจายยอดพร้อมทาน 99.- (Condition Zone) ---
    if "621822" in raw_data:
        val99 = raw_data.pop("621822")
        for c, r in {"231259": 0.5, "654830": 0.3, "724276": 0.2}.items():
            if c not in raw_data: raw_data[c] = {"q": 0, "a": 0}
            raw_data[c]["q"] += val99["q"] * r
            raw_data[c]["a"] += val99["a"] * r

    df = pd.DataFrame([
        {
            "หมวด": CATEGORY_MAP[c],
            "ชื่อสินค้า": PRODUCT_NAMES[c],
            "จำนวน": v["q"],
            "ราคาเดิม": v["a"],
            "ราคา+VAT": round(v["a"] * 1.07, 2)
        } for c, v in raw_data.items()
    ])

    if not df.empty:
        if page == "📊 สรุปภาพรวม":
            st.title("Daily Sales Report")
            
            total_net = df["ราคาเดิม"].sum()
            total_vat = df["ราคา+VAT"].sum()
            
            # สรุปตัวเลขสำคัญ (อ่านง่ายขึ้น 100%)
            m1, m2, m3 = st.columns(3)
            m1.metric("ยอดรวมเดิม (ก่อน VAT)", f"{total_net:,.2f}")
            m2.metric("ยอดสุทธิ (+VAT 7%)", f"{total_vat:,.2f}")
            m3.metric("Achievement", f"{(total_vat/170000)*100:.2f}%")

            # ตารางสรุปรายสเตชั่นตามที่สั่ง
            st.subheader("🏢 ยอดรวมแยกรายสเตชั่น")
            station_sum = df.groupby("หมวด")[["ราคาเดิม", "ราคา+VAT"]].sum().reset_index()
            st.table(station_sum.style.format("{:,.2f}", subset=["ราคาเดิม", "ราคา+VAT"]))

            # ตารางรวมสินค้าทั้งหมด
            st.subheader("📋 รายละเอียดสินค้า")
            st.dataframe(df.sort_values("จำนวน", ascending=False), use_container_width=True)

        else:
            cat_name = "ทานเล่น" if "ทานเล่น" in page else "พร้อมทาน"
            st.title(f"📍 Station: {cat_name}")
            sub_df = df[df["หมวด"] == cat_name].sort_values("จำนวน", ascending=False)
            
            st.table(sub_df.style.format("{:,.2f}", subset=["ราคาเดิม", "ราคา+VAT"]))
else:
    st.info("กรุณาอัปโหลดรูปภาพที่แถบเมนูด้านซ้าย")
